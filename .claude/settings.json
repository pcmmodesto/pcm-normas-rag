{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git status --porcelain | grep -q . && git add -A && git commit -m \"Auto-commit: Claude Code $(date +\\\"%Y-%m-%d %H:%M\\\")\" && git push origin HEAD || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
